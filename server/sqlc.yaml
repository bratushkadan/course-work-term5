---
version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "./db/sql/schemas/schema.sql"
    queries:
      - "./db/sql/queries/user.sql"

    gen:
      go:
        package: "database"
        out: "./generated/database"
        emit_json_tags: true
        # emit_interface: true
